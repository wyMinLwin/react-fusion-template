import { ReactNode } from "react";
type LogoutDialogProps = {
    children: ReactNode;
};
declare const LogoutDialog: ({ children }: LogoutDialogProps) => import("react/jsx-runtime").JSX.Element;
export default LogoutDialog;
